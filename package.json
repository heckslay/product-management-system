{
  "dependencies": {
    "node-sass": "^4.13.1"
  },
  "scripts": {
    "build:sass": "node-sass -r --output-style compressed web/assets/scss/styles.scss -o web/assets/dist",
    "watch:sass": "npm run build:sass && npm run build:sass -- -w"
  }
}
